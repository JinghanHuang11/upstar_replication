[pytest]
# Use project-local tmp directory instead of system Temp
tmp_path = .pytest_tmp
# Cache directory in project root
cache_dir = .pytest_cache
# Don't capture logging (so we see phase output)
log_cli = false
